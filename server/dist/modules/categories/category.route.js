"use strict";
// import express from 'express';
// import * as CategoryController from './category.controller';
// import { upload } from '../../middleware/upload';
// const router = express.Router();
// // Apply auth middleware to all category routes
// // router.use(authMiddleware);
// // Add category with file upload
// router.post('/add-category', upload.single('categoryImg'), CategoryController.AddCategoryController);
// // Get categories with pagination, search and filter
// router.get('/get-categories', CategoryController.GetCategoriesController);
// // Delete category
// router.delete('/delete-category/:categoryId', CategoryController.DeleteCategoryController);
// // Update category with optional file upload
// router.put('/edit-category/:categoryId', upload.single('categoryImg'), CategoryController.UpdateCategoryController);
// export default router;
